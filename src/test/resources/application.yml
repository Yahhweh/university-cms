spring:
  datasource:
    hikari:
      minimum-idle: 1
      maximum-pool-size: 10
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:14.6:///test?currentSchema=uni-cms&TC_REUSABLE=true
    username: root
    password: test


  flyway:
    enabled: true
    locations: classpath:/db/migration
    target: latest

subject:
  generator:
    min-per-lecturer: 1
    max-per-lecturer: 3







